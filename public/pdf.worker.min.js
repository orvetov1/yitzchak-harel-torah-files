
// PDF.js Worker v4.0.269 (minified)
// This is a simplified version for development
// In production, use the full worker from pdf.js
if (typeof window === 'undefined') {
  // Web Worker environment
  importScripts('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.269/pdf.worker.min.js');
} else {
  // Fallback for direct loading
  console.log('PDF Worker loaded as fallback');
}
